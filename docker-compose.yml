services:
  gridguard-api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - LLM_PROVIDER=${LLM_PROVIDER:-mock}
    volumes:
      - ./rag/index:/app/rag/index
    restart: unless-stopped
